#include <bits/stdc++.h>
using namespace std;
int main() {
    int add=0,sum=0;
    for(int i=1;i<=100;i++){
        sum+=i*i;
    }
    for(int i=0;i<=100;i++){
        add+=i;
    }
    add*=add;
    cout<<add-sum;
    return 0;
}
